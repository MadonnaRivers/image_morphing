# ForensicsSAM + AI-vs-Human Image Detector — combined dependencies
# Python 3.8–3.11. Install from repo root: pip install -r requirements.txt
# CPU-only: pip install torch torchvision (no CUDA). GPU: install PyTorch with your CUDA version from pytorch.org

# --- Core (ForensicsSAM) ---
torch>=2.0.0
torchvision>=0.15.0
timm
numpy
opencv-python
pandas
tqdm
matplotlib

# --- API ---
fastapi
uvicorn[standard]

# --- Weights download (download_weights.py) ---
gdown
huggingface_hub

# --- Segment Anything (build_sam.py) ---
icecream

# --- Image classifier (image_classifier/) ---
# Used when running predict.py; API calls it as subprocess (can use own venv).
transformers
Pillow
accelerate
